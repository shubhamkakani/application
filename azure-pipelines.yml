trigger:
- main

pool:
  name: my-agent-pool   # Your self-hosted agent pool

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy you
